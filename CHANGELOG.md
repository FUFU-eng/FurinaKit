# 更新日志 (Changelog)

所有关于 FurinaKit (芙宁娜工具箱) 的重要版本迭代与技术改进均记录于此。

---

## [2.0.4] - 待发布 (Upcoming)

### 核心亮点
- **离线后台 Worker 架构健壮性加固**：引入跨平台系统退出信号捕获与优雅停机（Graceful Shutdown）机制，软件关闭或退出时确保正在执行的重型任务安全收尾，防止生成残留垃圾文件或卡死任务状态。
- **敏感凭据脱敏防护**：底层日志与异常堆栈全面集成连接凭据正则脱敏逻辑，彻底防止 Redis 密码在日志中明文泄露。
- **动态状态自愈与异常诊断细分**：优化服务探测逻辑，支持分布式与自建服务环境下 Redis 发生网络抖动或重启后的 30 秒动态重连恢复自愈；细分异常分类，拒绝静默吞错。
- **在线更新器安全防护与规范重构**：在线升级链路新增严格的 URL 格式与 HTTPS 安全协议校验；统一标准异步异常流，并在 `finally` 块中 100% 确保超时定时器安全回收，杜绝游离计时器；缓存语义化版本解析结果，消除重复开销。

### 详细变更
- **Worker 优雅停机**：注册 `SIGINT`、`SIGTERM` 及 Windows `SIGBREAK` 信号监听，退出时平滑等待当前活跃任务处理完成，保障文件队列与本地存储数据一致性。
- **凭据脱敏安全机制**：实现 `mask_sensitive_url` 脱敏处理函数，净化队列读取失败及全局异常日志中的连接敏感信息。
- **连接自愈重试**：修复启动时单次固化探测结果的缺陷，增加探测冷却机制，支持 Redis 恢复后自动从本地队列切回分布式队列。
- **更新器安全加固与代码重构**：
  - `updater.ts` 增加 `isValidUpdateUrl` 安全过滤器，严格校验更新地址合法性与安全协议；
  - 超时计时器转移至 `finally` 块中，杜绝异常跳出导致的后台游离计时器；
  - 规范化异步调用，剔除 `await` 与 `.catch` 混用坏味道；
  - 预缓存当前应用版本解析结果，提升版本比对执行效率。

---

## [2.0.3] - 2026-09-10

### 核心亮点
- 彻底修复 B 站与全网视频提取下载在桌面打包环境下卡死在 40% 的致命死锁 Bug。
- 重构视频下载执行链路：改由 Node.js 服务层原生调度内置 `yt-dlp.exe` 与 `ffmpeg.exe`，彻底切断 PyInstaller 二进制调用空 Worker 的死锁陷阱。
- 实现真实流式进度上报：实时解析标准输出流百分比，前端进度条平滑从 0% 流转至 100%。
- 优化 DASH 分块流自动合并与纯音频 MP3 提取，全面提升国内（Bilibili）与海外主流平台音视频下载成功率。
- 清理历史构建冗余产物与临时文件，安装包与运行目录瘦身优化。

### 详细变更
- **视频下载引擎重构**：排查并彻底拔除 PyInstaller 打包环境下 `sys.executable -m yt_dlp` 语法无法识别导致子进程无限等待队列的恶性 Bug。在 Node.js 服务端层通过原生 `child_process.spawn` 直接调用随软件打包的独立 `yt-dlp.exe` 与 `ffmpeg.exe`。
- **进度监听与状态回显**：增加毫秒级标准输出解析逻辑，将 yt-dlp 原生下载百分比动态映射至任务队列状态流，前端实时同步显示下载进度、速率与剩余时间。
- **多平台与 DASH 合流优化**：针对 B 站等平台分离的音视频轨道，自动通过内置 FFmpeg 无损混流封装为 MP4；支持封面一键导出及纯音频提取。
- **环境与依赖瘦身**：剔除上一版本历史遗留测试解压目录与废弃调试文件，提升系统整体纯净度与运行效率。

---

## [2.0.2] - 2026-09-09

### 核心亮点
- 彻底修复桌面打包环境下缺失原生图像处理运行时（@img/sharp-win32-x64）导致的图片格式转换、调整大小、压缩等 500 报错异常。
- 修复 PDF 转 Word、Office 文档互转等重型离线 Worker 任务队列调度与通信环境变量（USE_FILE_QUEUE、NEXT_PUBLIC_ENABLE_HEAVY_WORKER_TOOLS）。
- 修复 B 站与全网音视频解析后下载任务异常，完善内置 FFmpeg 与跨进程调用链路。
- 优化主进程启动时本地存储目录预创建机制（storage/jobs、storage/queue、storage/uploads、storage/results），保障首次运行与多任务并发稳定性。
- 新增全自动在线版本检测与一键极速下载安装支持。

### 详细变更
- **图像处理引擎**：将 @img/sharp-win32-x64 二进制原生模块显式固化进构建输出链，杜绝外部机器运行报 Unexpected token '<' 问题。
- **离线 Worker 调度**：修正任务派发通道与心跳握手机制，确保高负载运算稳定由独立 Python 守护进程接管。
- **媒体工具链**：完善打包资源目录中 `ffmpeg.exe`、`ffprobe.exe` 与 `yt-dlp.exe` 的绝对定位解析，提升视频解析与音视频提取成功率。
- **环境自检与容灾**：启动时自动校验工作目录与文件写权限，异常捕获更健全，提示信息更加友好清晰。

---

## [2.0.1] - 2026-09-09

### 核心亮点
- 全新上线「艺术与电子签名设计器」，支持 62 种流派一笔艺术签、压感手写板、国风仿古印章铭刻及无白边透明 PNG 导出。
- 「观影读书记录器」升级为「影视 · 图书 · 番剧」三位一体文娱记录中心，新增追更打卡管理。
- 重构「全功能科学与程序员计算器」，支持 HEX/DEC/OCT/BIN 多进制按位运算与复杂科学函数。
- 优化工具箱分类架构与滚动历史记忆，退出工具详情页自动定位回原视口。

---

## [2.0.0] - 2026-09-04

### 核心亮点
- 基于 Electron + Next.js 独立桌面架构全新重构，带来全新芙宁娜水元素玻璃拟态设计语言。
- 100+ 款离线工具全量就绪，内置 Python 全能 Worker 与 FFmpeg 媒体处理套件。
- 双核驱动架构：内置独立便携 Node.js 服务端与 Python 媒体运算 Worker，支持完全离线脱网运行。
