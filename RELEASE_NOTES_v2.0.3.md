# FurinaKit v2.0.3 发布说明 (Release Notes)

> **版本**：v2.0.3  
> **发布日期**：2026-09-10  
> **适用平台**：Windows 10 / 11 (64位)  
> **安装包名**：`FurinaKit Setup 2.0.3.exe` (或 `FurinaKit.Setup.2.0.3.exe`)

---

## 🌟 本次更新核心 (Overview)

本次 v2.0.3 为**重大紧急质量与稳定性修复版本**。核心解决桌面打包环境下**全网与 B 站音视频下载任务卡死在 40%** 的顽疾死锁，彻底重构媒体执行底层；同时完善多媒体 DASH 分块流自动混流合并，并对安装包进行了深度冗余清理与运行加速。

---

## 🛠️ 关键修复与技术改进

### 1. 彻底根除「视频下载卡死在 40%」的致命死锁
- **【根因排查】**：经底层深度排查，旧版本在桌面安装包环境下调用了外部 Python 子进程的 `-m yt_dlp` 模块。而 PyInstaller 独立编译出的二进制 Worker 并无法识别 `-m` 模块语法，导致子进程无限派生空 Worker 并等待任务队列，既不报错也不输出任何下载进度，造成读取管道无限期死锁，进度条永远停滞在 40%。
- **【彻底重构】**：彻底切断脆弱的 Python 跨进程调用链，改由 Node.js 服务层原生 `spawn` 直接调度打包内置的独立可执行文件 `yt-dlp.exe` 与 `ffmpeg.exe`。
- **【实机验证】**：下载链路脱离外部 Python 环境，彻底解决进程死锁，全流程 100% 顺畅跑通。

### 2. 毫秒级真实流式下载进度回显
- 实时捕获解析底层下载 stdout 文本流，毫秒级提取下载百分比、下载速率与剩余时间；
- 进度条告别“卡死/虚假步进”，从 0% -> 解析连接 -> 分块下载 -> 100% 自动合并封装，全程丝滑流转。

### 3. 多平台与音视频轨道自动混流
- 完善针对 Bilibili（BV号/分P）、YouTube、X (Twitter)、TikTok 等主流平台的媒体流提取；
- 针对平台分离的最高画质视频流与音频流，自动调用内置 FFmpeg 进行无损 DASH 混流合并并输出标准 MP4；
- 完善纯音频（MP3）与最高画质封面（JPG）的独立一键提取与导出。

### 4. 软件体积瘦身与底层路径寻址加固
- 完善主进程与服务层对打包内置 `resources/` 目录下 `ffmpeg.exe` 与 `yt-dlp.exe` 的双重容灾寻址逻辑，杜绝任何环境丢失与路径找不到问题；
- 彻底清理上一版本沉淀的历史打包缓存与调试暂存文件，大幅降低软件包体积，提升系统整体纯净度与运行效率。

### 5. 彻底拦截点击下载后软件窗口被全屏播放器覆盖的异常
- **【根因修复】**：针对直接点击音视频链接时 Chromium 默认将其当作媒体文档（MediaDocument）渲染导致顶层窗口界面被替换的问题，在 Electron 网络层增加协议拦截，强制将页面级音视频请求转为 `attachment` 附件下载；
- **【路由守卫】**：主窗口全面启用 `will-navigate` 路由锁，坚决禁止主窗口脱离应用 SPA 界面；
- **【体验升级】**：任务悬浮球与下载页面新增「打开保存目录」一键直达，支持在 Windows 资源管理器中秒开文件所在位置。

---

## 📦 升级与安装指南

### 方式一：已有版本用户覆盖升级（推荐）
- 直接下载下方 Releases 中的 **`FurinaKit Setup 2.0.3.exe`**；
- 运行安装程序，**无需卸载旧版本**，直接覆盖安装即可，您的所有个性化设置与历史任务数据将完整保留。

### 方式二：全新安装
- 下载 **`FurinaKit Setup 2.0.3.exe`**；
- 按照向导选择安装路径并勾选“创建桌面快捷方式”，10 秒内即可安装完成。

---

## 🙏 致谢与致敬 (Special Thanks)

特别感谢开源开发者 **[Abdullah (@Abudora-0)](https://github.com/Abudora-0)** 及其优秀作品 **OmniKit**。FurinaKit 的桌面双核驱动理念与工程架构深受其开创性工作的启发。向优秀的开源先驱致以诚挚的感谢与崇高敬意！

---

## 📥 下载文件清单
- **Windows 64位 安装包**：`FurinaKit Setup 2.0.3.exe`
